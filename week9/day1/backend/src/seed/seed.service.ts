"import { Injectable, Logger, OnApplicationBootstrap } from '@nestjs/common';\nimport { InjectModel } from '@nestjs/mongoose';\nimport { Model } from 'mongoose';\nimport { ResearchDocument, DocumentDocument } from '../schemas/document.schema';\n\nconst SEED_DOCUMENTS = [\n  // ─── SQL vs NoSQL ────────────────────────────────────────────────────────\n  {\n    title: 'SQL Databases and ACID Properties',\n    topic: 'Databases',\n    tags: ['sql', 'relational', 'acid', 'database', 'transactions'],\n    content: `SQL (Structured Query Language) databases are relational database management systems that organize data into structured tables with predefined schemas. They are built around the ACID properties: Atomicity, Consistency, Isolation, and Durability. These properties ensure that database transactions are processed reliably.\n\nAtomicity guarantees that either all operations in a transaction succeed or none do. Consistency ensures that a transaction brings the database from one valid state to another. Isolation ensures that concurrent transactions execute as if they were sequential. Durability means that committed transactions remain committed even after system failure.\n\nPopular SQL databases include PostgreSQL, MySQL, Microsoft SQL Server, and SQLite. SQL databases excel at handling complex queries, enforcing referential integrity through foreign keys, and managing highly structured data. They use JOIN operations to combine data from multiple tables efficiently.\n\nSQL databases scale vertically — meaning you add more CPU, RAM, or storage to a single server. This makes them predictable in behavior but can become expensive at very large scales. However, modern SQL databases like PostgreSQL support read replicas and partitioning for better horizontal scalability.\n\nUse cases for SQL databases include financial systems, e-commerce platforms, ERP systems, and any application where data integrity
<truncated 29101 bytes>